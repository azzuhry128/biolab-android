class ServiceDetailModel {
  final String banner;
  final String title;
  final String price;
  final String description;

  const ServiceDetailModel(
      this.banner, this.title, this.price, this.description);
}

// final servicedetailmodels = List.generate(3, (index) => ServiceDetailModel(banner, title, price, description))
